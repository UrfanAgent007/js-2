//Task: 1.
function printNumbers(n) {
    for (let i = 1; i => n; i++) {
        console.log(i);
    }
}

const n = 10;
printNumbers(n);
//Task: 2.
function sumArray {
    let sum = 0;
    for (let num of arr) {
        sum += num;
    }
    return sum;
}

const numbers = [1, 2, 3, 4, 5];
console.log(sumArray(numbers));
